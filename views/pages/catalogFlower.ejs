<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <title><%= head.title %></title>

    <link rel="stylesheet" href="../css/bootstrap.min.css">
    <link rel="stylesheet" href="../css/bootstrap-theme.min.css">
    <link rel="stylesheet" href="../css/style.css">
    <link rel="stylesheet" href="../css/font-awesome.min.css">
    <link rel="shortcut icon" href="../resource/icon.png" type="image/png">
    <script data-main="../js/catalogItem.js" src="/js/lib/require.js" type="text/javascript"></script>
</head>
<body>
<header>
    <% include ../partials/header.ejs %>
</header>

<div class="container margin-top">
    <div class="header italic left">
        <h1 id="flowerName" data-id="<%= data._id %>"><%= data.name %></h1>
    </div>
    <div class="row">
        <div class="col-lg-2">
            <img class="catalog-item-image" src="../resource/catalog/<%= data.image %>" alt="<%= data.name %>">
        </div>
        <div class="col-lg-6 col-lg-offset-1">
            <p>Стоимость: <b><span id="descriptionPrice"><%= data.price %></span> руб.</b></p>
            <% data.description.forEach(function(item) { %>
            <p><%= item %></p>
            <% }); %>
        </div>
        <div class="col-lg-3">
            <div class="order-form">
                <span id="header">Высота цветов:</span>
                <select id="roseHeight" name="roseHeight" class="form-control">
                    <option value="50">50 см</option>
                    <option selected="selected" value="60">60 см</option>
                    <option value="70">70 см</option>
                    <option value="80">80 см</option>
                    <option value="90">90-100</option>
                </select>
                <span id="header">Количество цветов:</span>
                <div id="countGroup" class="input-group">
                    <span class="input-group-btn">
                        <button id="minus" class="btn btn-secondary" type="button">-</button>
                    </span>
                    <input id="count" type="text" value="<%= data.count %>" class="form-control"
                           placeholder="Количество роз">
                    <span class="input-group-btn">
                        <button id="plus" class="btn btn-secondary" type="button">+</button>
                    </span>
                </div>
                <span>+ доставка 5 руб.<br>При заказе свыше 51 шт.<br> <b>доставка бесплатно!</b></span>
                <p id="priceText">Цена с доставкой: <b><span id="finalPrice"><%= data.price %></span> руб.</b></p>
                <button id="makeOrderBtn" class="btn btn-success" type="button">Оформить заказ</button>
            </div>
        </div>
    </div>
    <div id="bouquets"></div>
</div>
</body>
</html>